<header class="site-header">
  <div class="inner">
    <a href="{{ .Site.BaseURL }}" class="brand">{{ .Site.Title }}</a>
    <nav class="main-nav">
      {{ range .Site.Menus.main }}
        <a href="{{ .URL }}">{{ .Name }}</a>
      {{ end }}
    </nav>
  </div>
</header>
